import QtQuick
import qs

// The default look of a module's label.
Text {
	color: Theme.fg
	font.family: Theme.fontFamily
	font.pixelSize: Theme.fontText
	textFormat: Text.PlainText
	renderType: Text.NativeRendering
}
